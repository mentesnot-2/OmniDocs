# Core
python-dotenv>=1.0.0

# Document parsing
pypdf>=4.0.0
python-docx>=1.0.0
openpyxl>=3.1.0
beautifulsoup4>=4.12.0

# Chunking & text processing
tiktoken>=0.5.0

# Embeddings (we'll use sentence-transformers for local, or OpenAI for cloud)
sentence-transformers>=2.2.0

# Vector store (we'll use Chroma - simple, local, no server needed)
chromadb>=0.4.0

# LLM (OpenAI API)
openai>=1.0.0

# Optional: FastAPI for API later
# fastapi>=0.100.0
# uvicorn>=0.22.0